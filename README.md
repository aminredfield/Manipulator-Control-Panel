# Manipulator Control Panel

Веб-приложение для управления лабораторным манипулятором с визуализацией движения и оптимизацией команд.

## 🔗 Демо

Публичная ссылка: [Добавьте вашу ссылку сюда]

## 📋 Описание проекта

Панель управления манипулятором представляет собой интерактивное веб-приложение для управления роботизированным манипулятором в лабораторной среде. Приложение позволяет:

- Управлять перемещением манипулятора по сетке лаборатории
- Работать с образцами (взять/отпустить)
- Визуализировать движение в реальном времени
- Автоматически оптимизировать последовательность команд
- Просматривать историю выполненных операций
- Настраивать параметры стола и скорость анимации

## 🛠 Технологический стек

### Frontend
- **React 18.3.1** - библиотека для построения пользовательского интерфейса
- **TypeScript 5.9.3** - типизированный JavaScript для надежности кода
- **Redux Toolkit 2.10.1** - управление состоянием приложения
- **React Redux 9.2.0** - интеграция Redux с React
- **Material-UI (MUI) 6.5.0** - компонентная библиотека с темой Material Design
- **Emotion** - CSS-in-JS для стилизации компонентов
- **React Hook Form 7.66.1** - управление формами

### Build Tools
- **Vite 5.4.21** - быстрый сборщик и dev-сервер
- **SWC** - быстрый компилятор JavaScript/TypeScript

### Особенности
- Темная тема интерфейса
- Адаптивный дизайн (responsive)
- Локальное хранилище для сохранения истории и состояния аутентификации
- Анимация выполнения команд

## 📦 Установка

### Предварительные требования

- Node.js версии 16.x или выше
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <url-репозитория>
cd manipulator-control-panel
```

2. Установите зависимости:
```bash
npm install
# или
yarn install
```

## 🚀 Запуск проекта

### Режим разработки

Запустите dev-сервер с hot-reload:

```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена

Создайте оптимизированную production-сборку:

```bash
npm run build
# или
yarn build
```

Собранные файлы будут находиться в папке `dist/`

### Предварительный просмотр production-сборки

После сборки можно просмотреть результат:

```bash
npm run preview
# или
yarn preview
```

## 🔐 Вход в систему

Для входа в систему используйте:
- **Логин:** `admin`
- **Пароль:** `admin`

## 📖 Руководство пользователя

### Основные команды манипулятора

Манипулятор управляется следующими командами:

- **Л** - шаг влево
- **П** - шаг вправо
- **В** - шаг вверх
- **Н** - шаг вниз
- **О** - взять образец (в текущей позиции)
- **Б** - отпустить образец (в текущей позиции)

### Примеры команд

```
ЛЛЛЛВВПППОНННБ
ЛЛЛНННЛЛЛНННО
ВПВПВПОБББ
```

### Работа с приложением

1. **Вход в систему**
   - Введите логин и пароль
   - Нажмите "Войти"

2. **Настройка стола**
   - Установите ширину (количество колонок): 1-30
   - Установите высоту (количество строк): 1-30
   - Установите количество образцов: 0-20
   - Нажмите "Перегенерировать образцы" для случайного размещения

3. **Отправка команд**
   - Введите последовательность команд в текстовое поле
   - Приложение автоматически покажет оптимизированную версию
   - Нажмите "Отправить на манипулятор"
   - Наблюдайте за анимацией выполнения

4. **Настройка скорости**
   - Используйте слайдер для изменения интервала между шагами (50-1000 мс)

5. **История операций**
   - Просматривайте все выполненные команды
   - Сравнивайте исходные и оптимизированные версии
   - Отслеживайте изменение позиций образцов

6. **Сброс состояния**
   - Нажмите кнопку с иконкой обновления для сброса манипулятора и генерации новых образцов

## 📊 Структура проекта

```
src/
├── app/                    # Конфигурация приложения
│   ├── hooks.ts           # Redux hooks
│   └── store.ts           # Настройка Redux store
├── components/            # Переиспользуемые компоненты
│   └── Layout.tsx         # Основной layout с навигацией
├── features/              # Функциональные модули
│   ├── api/              # API интеграция
│   ├── auth/             # Аутентификация
│   └── manipulator/      # Логика манипулятора
│       ├── commands.ts   # Обработка и оптимизация команд
│       ├── types.ts      # TypeScript типы
│       ├── manipulatorSlice.ts  # Redux состояние
│       ├── historySlice.ts      # История операций
│       └── компоненты UI
├── pages/                # Страницы приложения
│   ├── DashboardPage.tsx
│   └── LoginPage.tsx
└── main.tsx              # Точка входа
```

## 🎨 Особенности реализации

### Оптимизация команд

Приложение автоматически оптимизирует последовательности команд двумя способами:

1. **RLE (Run-Length Encoding)** - сжатие повторяющихся команд
   - `ЛЛЛЛ` → `4Л`

2. **Обнаружение повторяющихся блоков**
   - `ЛВЛВЛВ` → `3(ЛВ)`

### Валидация

- Проверка допустимых символов команд
- Контроль границ перемещения манипулятора
- Валидация параметров стола и образцов

### Анимация

- Плавная визуализация выполнения команд
- Настраиваемая скорость анимации
- Визуальное отображение состояния манипулятора и образцов

## 💾 Локальное хранилище

Приложение сохраняет в localStorage:
- Статус аутентификации
- Историю выполненных команд

## 🌐 Браузерная совместимость

Приложение протестировано и работает в:
- Chrome/Edge (последние версии)
- Firefox (последние версии)
- Safari (последние версии)

## 📱 Адаптивность

Интерфейс адаптируется под различные размеры экранов:
- Desktop (> 900px)
- Tablet (600-900px)
- Mobile (< 600px)

## 🔧 Разработка

### Добавление новых команд

Для добавления новых команд манипулятора:

1. Обновите тип `CommandChar` в `src/features/manipulator/types.ts`
2. Добавьте обработку команды в функцию `applyCommand` в `src/features/manipulator/commands.ts`
3. Обновите описание в UI компоненте `ControlsForm.tsx`


## 👥 Автор

https://github.com/aminredfield

